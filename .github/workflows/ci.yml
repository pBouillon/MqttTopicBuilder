name: .NET CI

on:
  pull_request:
    branches: [main, master]

  push:
    branches: [main, master]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: "9.x"

      - name: Restore dependencies
        run: dotnet restore MqttTopicBuilder.slnx

      - name: Build
        run: dotnet build MqttTopicBuilder.slnx --no-restore

      - name: Test
        run: dotnet test MqttTopicBuilder.slnx --no-build --verbosity normal
